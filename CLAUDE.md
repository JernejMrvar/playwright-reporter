# playwright-reporter

## After making changes

Always build before committing — the `dist/` folder is committed and must stay in sync with `src/`:

```bash
npm run build
```

Then commit both `src/` and `dist/` together.
